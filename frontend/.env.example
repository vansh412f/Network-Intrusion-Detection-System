# Backend API URL (set to your Render backend URL on Netlify)
VITE_API_URL=http://localhost:3000

# Sensor URL (set to your Render sensor URL on Netlify — used to wake up the sensor on page load)
VITE_SENSOR_URL=https://your-nids-sensor.onrender.com